IMPORT STEPS (Android Studio):
1) File > Open... and select the KOIMart folder.
2) Allow Gradle to sync.
3) If Gradle wrapper jar is missing, Android Studio usually regenerates it; if not, run: gradle wrapper (from terminal) then sync.
